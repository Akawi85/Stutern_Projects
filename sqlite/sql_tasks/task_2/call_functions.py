#!/usr/bin/env python3

# uncomment the lines you wish, to see what it does

import insurance_app

#insurance_app.get_females(n) # returns n rows for just females for the age, bmi, gender and kids column

#insurance_app.delete_one(n) # deletes one item given the n row id

#insurance_app.get_males(n) # returns n rows for just males for the age, bmi, gender and kids column

#insurance_app.four_columns_n_rows(n) # returns the age, bmi, gender and kids column for n rows

#insurance_app.show_all() # Query the DB and Return all RECORDS (Takes no arguments)

print("\n command executed successfully....")